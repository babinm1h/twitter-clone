import React from 'react';
import BackButton from '../BackButton/BackButton';

const Profile = () => {
    return (
        <>
            <div className="home__main_header">
                <BackButton /><h2>profile</h2>
            </div>
        </>
    );
};

export default Profile;